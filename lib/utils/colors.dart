import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF1565C0);      // Dark Blue
  static const Color primaryLight = Color(0xFF1E88E5); // Light Blue
  static const Color accent = Color(0xFF00ACC1);        // Teal

  // Status Colors
  static const Color pending = Color(0xFFFFA726);       // Orange
  static const Color accepted = Color(0xFF42A5F5);      // Blue
  static const Color inProcess = Color(0xFF7E57C2);     // Purple
  static const Color waitingParts = Color(0xFFEF5350);  // Red
  static const Color completed = Color(0xFF66BB6A);     // Green

  // Neutral Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color white = Color(0xFFFFFFFF);
  static const Color textDark = Color(0xFF212121);
  static const Color textGray = Color(0xFF757575);
  static const Color border = Color(0xFFE0E0E0);
}